//
//  Show.swift
//  KexpTVStream
//
//  Created by Dustin Bergman on 2/2/17.
//  Copyright © 2017 Dustin Bergman. All rights reserved.
//

/*
import SwiftyJSON

public struct Shows {
    let count: Int
    let next: URL?
    let previous: URL?
    let theShows: [Show]?
    
    public init(_ shows: JSON) {
        count = shows["count"].intValue
        next = shows["next"].url
        previous = shows["previous"].url
        
        if let showResults = shows["results"].array {
            var shows = [Show]()
            
            for show in showResults {
                shows.append(Show(show))
            }
            
            theShows = shows
        }
        else {
            theShows = nil
        }
    }
}

*/
